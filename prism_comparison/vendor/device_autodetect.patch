diff -ruN a/scripts/localization.py b/scripts/localization.py
--- a/scripts/localization.py	2026-07-19 11:02:31
+++ b/scripts/localization.py	2026-07-19 11:02:31
@@ -1,6 +1,7 @@
 import os
 import numpy as np
 import torch
+DEVICE = "cuda:0" if torch.cuda.is_available() else "cpu"  # [prism_comparison] auto device
 import time
 from utils import apply_pose_shift
 from copy import deepcopy
@@ -32,7 +33,7 @@
         if self.tests_dir is not None and not os.path.exists(self.tests_dir):
             os.mkdir(self.tests_dir)
         self.mutex = Lock()
-        self.device = torch.device('cuda:0')
+        self.device = torch.device(DEVICE)
 
     def save_reg_test_data(self, vertex_ids, transforms, pr_scores, reg_scores, save_dir):
         if not os.path.exists(save_dir):
diff -ruN a/scripts/models.py b/scripts/models.py
--- a/scripts/models.py	2026-07-19 11:02:31
+++ b/scripts/models.py	2026-07-19 11:02:31
@@ -1,6 +1,7 @@
 from hydra.utils import instantiate
 from omegaconf import OmegaConf
 import torch
+DEVICE = "cuda:0" if torch.cuda.is_available() else "cpu"  # [prism_comparison] auto device
 import faiss
 import opr
 from opr.pipelines.registration import PointcloudRegistrationPipeline, RansacGlobalRegistrationPipeline
@@ -14,8 +15,8 @@
     WEIGHTS_PATH = os.path.join(opr_source_dir, config['weights_path'])
     if config['model'] == 'minkloc3d':
         model = MinkLoc3D()
-        model.load_state_dict(torch.load(WEIGHTS_PATH))
-        model = model.to("cuda")
+        model.load_state_dict(torch.load(WEIGHTS_PATH, map_location=DEVICE))
+        model = model.to(DEVICE)
         model.eval()
         index = faiss.IndexFlatL2(256)
         return model, index
@@ -23,9 +24,9 @@
         MODEL_CONFIG_PATH = os.path.join(opr_source_dir, config['model_config_path'])
         model_config = OmegaConf.load(MODEL_CONFIG_PATH)
         model = instantiate(model_config)
-        load = torch.load(WEIGHTS_PATH)
+        load = torch.load(WEIGHTS_PATH, map_location=DEVICE)
         model.load_state_dict(load)#torch.load(WEIGHTS_PATH)['model_state_dict'])
-        model = model.to("cuda")
+        model = model.to(DEVICE)
         model.eval()
         index = faiss.IndexFlatL2(512)
         return model, index
@@ -48,7 +49,7 @@
         REGISTRATION_MODEL_CONFIG_PATH = config['model_config_path']
         REGISTRATION_WEIGHTS_PATH = config['weights_path']
         registration_model = instantiate(OmegaConf.load(REGISTRATION_MODEL_CONFIG_PATH))
-        registration_model.load_state_dict(torch.load(REGISTRATION_WEIGHTS_PATH))
+        registration_model.load_state_dict(torch.load(REGISTRATION_WEIGHTS_PATH, map_location=DEVICE))
         return PointcloudRegistrationPipeline(model=registration_model,
                                               model_weights_path=REGISTRATION_WEIGHTS_PATH,
                                               device="cuda",  # the GeoTransformer currently only supports CUDA
diff -ruN a/scripts/prism_topomap.py b/scripts/prism_topomap.py
--- a/scripts/prism_topomap.py	2026-07-19 11:02:31
+++ b/scripts/prism_topomap.py	2026-07-19 11:02:31
@@ -5,6 +5,7 @@
 import time
 import yaml
 import torch
+DEVICE = "cuda:0" if torch.cuda.is_available() else "cpu"  # [prism_comparison] auto device
 from localization import Localizer
 from utils import *
 from topo_graph import TopologicalGraph
@@ -60,7 +61,7 @@
         self.found_loop_closure = False
         self.need_to_change_vcur = False
         self.path = []
-        self.device = torch.device('cuda:0')
+        self.device = torch.device(DEVICE)
 
         self.graph = TopologicalGraph(place_recognition_index=self.place_recognition_index,
                                       inline_registration_model=self.inline_registration_model,
@@ -473,15 +474,15 @@
     def process_observations(self, img_front, img_back, cur_cloud, cur_curbs, x, y, theta):
         # Extract descriptor from cloud and images
         input_data = {
-                     'pointcloud_lidar_coords': torch.Tensor(cur_cloud[:, :3]).cuda(),
-                     'pointcloud_lidar_feats': torch.ones((cur_cloud.shape[0], 1)).cuda(),
+                     'pointcloud_lidar_coords': torch.Tensor(cur_cloud[:, :3]).to(DEVICE),
+                     'pointcloud_lidar_feats': torch.ones((cur_cloud.shape[0], 1)).to(DEVICE),
                      }
         if img_front is not None:
-            img_front_tensor = torch.Tensor(img_front).cuda()
+            img_front_tensor = torch.Tensor(img_front).to(DEVICE)
             img_front_tensor = torch.permute(img_front_tensor, (2, 0, 1))
             input_data['image_front'] = img_front_tensor
         if img_back is not None:
-            img_back_tensor = torch.Tensor(img_back).cuda()
+            img_back_tensor = torch.Tensor(img_back).to(DEVICE)
             img_back_tensor = torch.permute(img_back_tensor, (2, 0, 1))
             input_data['image_back'] = img_back_tensor
         batch = self._preprocess_input(input_data)
diff -ruN a/scripts/topo_graph.py b/scripts/topo_graph.py
--- a/scripts/topo_graph.py	2026-07-19 11:02:31
+++ b/scripts/topo_graph.py	2026-07-19 11:02:31
@@ -8,6 +8,7 @@
 
 import faiss
 import torch
+DEVICE = "cuda:0" if torch.cuda.is_available() else "cpu"  # [prism_comparison] auto device
 from scipy.spatial.transform import Rotation
 from skimage.io import imread, imsave
 from opr.pipelines.registration import PointcloudRegistrationPipeline, RansacGlobalRegistrationPipeline, Feature2DGlobalRegistrationPipeline
@@ -33,7 +34,7 @@
         self.grid_resolution = grid_resolution
         self.grid_radius = grid_radius
         self.max_grid_range = max_grid_range
-        self.device = torch.device('cuda:0')
+        self.device = torch.device(DEVICE)
 
     def normalize(self, angle):
         while angle < -np.pi:
