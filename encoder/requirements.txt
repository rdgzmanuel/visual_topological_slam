# Dependencies for encoder training and evaluation (encoder/src).
# For CUDA builds install torch/torchvision from the matching index, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16
numpy
pillow
scikit-learn
matplotlib
umap-learn
tensorboard
tqdm
requests
